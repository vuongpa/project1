import { withClasses } from '../../react_utils';
import { DefaultLayout as DL } from './default-layout';
import classes from './default-layout.module.scss';

export const DefaultLayout = withClasses(classes, DL);
