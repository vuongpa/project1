import { withClasses } from '../../react_utils';
import { AuthLayout as AL } from './auth-layout';
import classes from './auth-layout.module.scss';

export const AuthLayout = withClasses(classes, AL);
