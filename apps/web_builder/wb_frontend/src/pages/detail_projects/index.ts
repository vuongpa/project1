import { withClasses } from '../../react_utils';
import { DetailProjectsPage as DPP } from './detail-projects';
import classes from './detail-projects.module.scss';

export const DetailProjectsPage = withClasses(classes, DPP);
