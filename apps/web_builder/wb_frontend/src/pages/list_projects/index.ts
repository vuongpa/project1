import { withClasses } from '../../react_utils';
import { ListProjectsPage as LPP } from './list-projects';
import classes from './list-projects.module.scss';

export const ListProjectsPage = withClasses(classes, LPP);
