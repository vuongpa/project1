import { withClasses } from '../../react_utils';
import { CreateProjectsPage as CPP } from './create-projects';
import classes from './create-projects.module.scss';

export const CreateProjectPage = withClasses(classes, CPP);
